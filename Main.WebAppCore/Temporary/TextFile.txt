## Core Architecture Highlights

### Zero Dependency Framework: 
Runs strictly inside pure Vanilla DOM logic without loading jQuery or external script overhead.

### Transition Layering: 
Rather than relying on pixel calculation widths, it uses an opacity CSS layout filter to give slides a smooth fade effect.

### Bounded Indexing: 
The conditional operators loop seamlessly from the last slide back to the first slide without throwing script reference errors.